/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

// Inherit the parent event
event_inherited();

// personalizar do pai
vel = choose(1, -1);

andando = choose(true, false)
tempo_de_anda = room_speed;

if (andando)
{
	vel = choose(vel, -vel);
}
else
{
	velh = 0;
}